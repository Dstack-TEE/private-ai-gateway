{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "maintenance",
  "description": "File selection for profile and diagnostics maintenance",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
