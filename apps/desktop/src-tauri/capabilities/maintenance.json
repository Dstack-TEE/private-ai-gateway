{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "maintenance",
  "description": "File selection for profile and diagnostics maintenance",
  "windows": ["main", "profiles"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
